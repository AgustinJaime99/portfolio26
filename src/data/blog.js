// Mock blog content. Bilingual (es/en) posts keyed by a stable `slug` so the
// hash router can resolve #/blog/:slug regardless of the active language.

export const blogMeta = {
  es: {
    eyebrow: 'Blog',
    title: 'Escribo sobre lo que ',
    titleHighlight: 'aprendo',
    intro:
      'Notas sobre arquitectura limpia, desarrollo full stack e integración de IA en productos reales. Aprendizajes que no solo quedan como recuerdo.',
    backToHome: 'Volver al inicio',
    backToBlog: 'Volver al blog',
    featured: 'Destacado',
    readMore: 'Leer artículo',
    minRead: 'min de lectura',
    allPosts: 'Todos los artículos',
    searchPlaceholder: 'Buscar artículos...',
    noResults: 'No se encontraron artículos.',
    share: 'Compartir',
    publishedOn: 'Publicado el',
    relatedTitle: 'Sigue leyendo',
    newsletterTitle: '¿Te gustó lo que leíste?',
    newsletterText: 'Recibe nuevos artículos sobre arquitectura, full stack e IA directo en tu correo.',
    newsletterCta: 'Suscribirme',
    newsletterPlaceholder: 'agustinjaime.dev@email.com',
  },
  en: {
    eyebrow: 'Blog',
    title: 'I write about what I ',
    titleHighlight: 'learn',
    intro:
      'Notes on clean architecture, full stack development and integrating AI into real products. Lessons that stay with me.',
    backToHome: 'Back to home',
    backToBlog: 'Back to blog',
    featured: 'Featured',
    readMore: 'Read article',
    minRead: 'min read',
    allPosts: 'All articles',
    searchPlaceholder: 'Search articles...',
    noResults: 'No articles found.',
    share: 'Share',
    publishedOn: 'Published on',
    relatedTitle: 'Keep reading',
    newsletterTitle: 'Liked what you read?',
    newsletterText: 'Get new articles about architecture, full stack and AI straight to your inbox.',
    newsletterCta: 'Subscribe',
    newsletterPlaceholder: 'agustinjaime.dev@email.com',
  },
}

// Each post: shared keys (slug, date, readingTime, tags, cover, accent, featured)
// plus per-language { title, excerpt, body[] }. Body blocks are simple typed
// nodes so the renderer stays a small switch — no markdown dependency.
export const posts = [
  {
    slug: 'clean-architecture-frontend',
    date: '2026-05-28',
    readingTime: 12,
    featured: true,
    tags: ['Architecture', 'Full Stack', 'Backend', 'Frontend'],
    cover: 'linear-gradient(135deg, #6366f1 0%, #a855f7 60%, #22d3ee 100%)',
    // Drop a file at public/images/blog/clean-architecture.png to replace the
    // gradient cover with a real image. Remove this line to use the gradient.
    coverImage: '/images/blog/clean-architecture.png',
    es: {
      title: 'Clean Architecture de punta a punta: front y back con las mismas reglas',
      excerpt:
        'La arquitectura limpia no es "cosa del backend". Las mismas ideas (dominio aislado, puertos y adaptadores) aplican igual en una API NestJS que en una SPA React. Te muestro ambos lados con código.',
      body: [
        { type: 'p', text: 'Durante años escuché que la arquitectura limpia "es cosa del backend". La realidad es que un frontend moderno tiene tanta lógica de negocio como cualquier servicio: validaciones, estados, orquestación de llamadas y reglas de dominio. La buena noticia: las mismas reglas que ordenan una API ordenan una SPA. Veámoslo en ambos lados.' },
        { type: 'h2', text: 'La idea central: dependencias que apuntan hacia adentro' },
        { type: 'p', text: 'Clean Architecture se reduce a una regla: el dominio no conoce a nadie, y todo lo demás lo conoce a él. La base de datos, el framework HTTP, React o el cliente de fetch son detalles que se enchufan por fuera mediante puertos (interfaces) y adaptadores (implementaciones). Cambias el detalle sin tocar el dominio.' },
        { type: 'quote', text: 'La arquitectura no se trata de carpetas, se trata de qué tan caro es cambiar de opinión.' },
        { type: 'h2', text: 'Backend: el dominio no conoce a Prisma' },
        { type: 'p', text: 'En el backend el error clásico es que el caso de uso importe Prisma directamente. En cuanto eso pasa, tu lógica de negocio quedó casada con un ORM. La solución es definir un puerto en el dominio y dejar que la infraestructura lo implemente.' },
        { type: 'code', lang: 'ts', text: '// domain/course.repository.ts — el PUERTO (solo una interfaz)\nexport interface CourseRepository {\n  findBySlug(slug: string): Promise<Course | null>\n  save(course: Course): Promise<void>\n}\n\n// application/enroll-student.usecase.ts — orquesta reglas, NO sabe de Prisma\nexport class EnrollStudent {\n  constructor(private readonly courses: CourseRepository) {}\n\n  async run(slug: string, student: Student): Promise<void> {\n    const course = await this.courses.findBySlug(slug)\n    if (!course) throw new CourseNotFound(slug)\n    course.enroll(student) // la regla vive en la entidad\n    await this.courses.save(course)\n  }\n}' },
        { type: 'code', lang: 'ts', text: '// infrastructure/prisma-course.repository.ts — el ADAPTADOR\nexport class PrismaCourseRepository implements CourseRepository {\n  constructor(private readonly db: PrismaClient) {}\n\n  async findBySlug(slug: string) {\n    const row = await this.db.course.findUnique({ where: { slug } })\n    return row ? CourseMapper.toDomain(row) : null\n  }\n\n  async save(course: Course) {\n    await this.db.course.upsert(CourseMapper.toPersistence(course))\n  }\n}' },
        { type: 'p', text: 'El caso de uso depende de la interfaz, no de la base de datos. Migrar de Prisma a TypeORM, o testear con un repositorio en memoria, no toca ni una línea del dominio.' },
        { type: 'h2', text: 'Frontend: el componente no conoce a fetch' },
        { type: 'p', text: 'Del lado del cliente el patrón es idéntico. Cuando un componente conoce la forma exacta de la respuesta de tu API, cualquier cambio en el contrato se propaga por toda la UI. El mismo puerto, ahora en el frontend, vuelve a salvar el día.' },
        { type: 'code', lang: 'ts', text: '// domain/course.repository.ts — la UI depende de un puerto, no de fetch\nexport interface CourseRepository {\n  findAll(): Promise<Course[]>\n  findBySlug(slug: string): Promise<Course | null>\n}\n\n// infrastructure/http-course.repository.ts — el adaptador HTTP\nexport class HttpCourseRepository implements CourseRepository {\n  async findAll() {\n    const res = await fetch(\"/api/courses\")\n    const dto = await res.json()\n    return dto.map(CourseMapper.toDomain) // DTO -> dominio en la frontera\n  }\n  findBySlug = async (slug: string) =>\n    CourseMapper.toDomain(await (await fetch(`/api/courses/${slug}`)).json())\n}' },
        { type: 'code', lang: 'tsx', text: '// presentation/useCourses.ts — el hook habla con el puerto, no con la red\nexport function useCourses(repo: CourseRepository) {\n  const [courses, setCourses] = useState<Course[]>([])\n  useEffect(() => { repo.findAll().then(setCourses) }, [repo])\n  return courses\n}\n\n// El componente solo renderiza dominio ya mapeado y validado\nfunction CourseList({ repo }: { repo: CourseRepository }) {\n  const courses = useCourses(repo)\n  return courses.map((c) => <CourseCard key={c.id} course={c} />)\n}' },
        { type: 'p', text: 'Fíjate en la simetría: front y back declaran el mismo puerto CourseRepository. En un monorepo incluso puedes compartir las entidades del dominio entre ambos, y el contrato deja de divergir.' },
        { type: 'h2', text: 'Donde se aplican estos conceptos' },
        { type: 'p', text: 'No creo carpetas domain/application/infrastructure en un CRUD de fin de semana. Pero en cuanto hay reglas que sobreviven a un rediseño de UI o a un cambio de base de datos, esas reglas merecen vivir aisladas. La prueba es simple: ¿podrías cambiar React por otra cosa, o Prisma por otra cosa, sin reescribir tu lógica? Si la respuesta es no, está demasiado acoplada.' },
        { type: 'p', text: 'En Tech Academy esto significó separar el dominio de cursos de su persistencia y de su UI. Cuando migramos de fetch a un cliente tipado en el front, y ajustamos el ORM en el back, ni un solo caso de uso cambió.' },
      ],
    },
    en: {
      title: 'Clean Architecture end to end: front and back, same rules',
      excerpt:
        'Clean architecture is not "a backend thing". The same ideas (isolated domain, ports and adapters) apply equally to a NestJS API and a React SPA. Here is both sides with code.',
      body: [
        { type: 'p', text: 'For years I heard that clean architecture was "a backend thing". The reality is that a modern frontend holds as much business logic as any service: validation, state, call orchestration and domain rules. The good news: the same rules that tidy an API tidy a SPA. Let us look at both sides.' },
        { type: 'h2', text: 'The core idea: dependencies point inward' },
        { type: 'p', text: 'Clean Architecture boils down to one rule: the domain knows no one, and everything else knows the domain. The database, the HTTP framework, React or the fetch client are details you plug in from the outside via ports (interfaces) and adapters (implementations). Swap the detail without touching the domain.' },
        { type: 'quote', text: 'Architecture is not about folders, it is about how expensive it is to change your mind.' },
        { type: 'h2', text: 'Backend: the domain does not know Prisma' },
        { type: 'p', text: 'On the backend the classic mistake is a use case importing Prisma directly. The moment that happens, your business logic is married to an ORM. The fix is to define a port in the domain and let infrastructure implement it.' },
        { type: 'code', lang: 'ts', text: '// domain/course.repository.ts — the PORT (just an interface)\nexport interface CourseRepository {\n  findBySlug(slug: string): Promise<Course | null>\n  save(course: Course): Promise<void>\n}\n\n// application/enroll-student.usecase.ts — orchestrates rules, knows NO Prisma\nexport class EnrollStudent {\n  constructor(private readonly courses: CourseRepository) {}\n\n  async run(slug: string, student: Student): Promise<void> {\n    const course = await this.courses.findBySlug(slug)\n    if (!course) throw new CourseNotFound(slug)\n    course.enroll(student) // the rule lives in the entity\n    await this.courses.save(course)\n  }\n}' },
        { type: 'code', lang: 'ts', text: '// infrastructure/prisma-course.repository.ts — the ADAPTER\nexport class PrismaCourseRepository implements CourseRepository {\n  constructor(private readonly db: PrismaClient) {}\n\n  async findBySlug(slug: string) {\n    const row = await this.db.course.findUnique({ where: { slug } })\n    return row ? CourseMapper.toDomain(row) : null\n  }\n\n  async save(course: Course) {\n    await this.db.course.upsert(CourseMapper.toPersistence(course))\n  }\n}' },
        { type: 'p', text: 'The use case depends on the interface, not on the database. Migrating from Prisma to TypeORM, or testing with an in-memory repository, touches not a single line of the domain.' },
        { type: 'h2', text: 'Frontend: the component does not know fetch' },
        { type: 'p', text: 'On the client the pattern is identical. When a component knows the exact shape of your API response, any change to the contract ripples through the whole UI. The same port, now on the frontend, saves the day again.' },
        { type: 'code', lang: 'ts', text: '// domain/course.repository.ts — the UI depends on a port, not on fetch\nexport interface CourseRepository {\n  findAll(): Promise<Course[]>\n  findBySlug(slug: string): Promise<Course | null>\n}\n\n// infrastructure/http-course.repository.ts — the HTTP adapter\nexport class HttpCourseRepository implements CourseRepository {\n  async findAll() {\n    const res = await fetch(\"/api/courses\")\n    const dto = await res.json()\n    return dto.map(CourseMapper.toDomain) // DTO -> domain at the boundary\n  }\n  findBySlug = async (slug: string) =>\n    CourseMapper.toDomain(await (await fetch(`/api/courses/${slug}`)).json())\n}' },
        { type: 'code', lang: 'tsx', text: '// presentation/useCourses.ts — the hook talks to the port, not the network\nexport function useCourses(repo: CourseRepository) {\n  const [courses, setCourses] = useState<Course[]>([])\n  useEffect(() => { repo.findAll().then(setCourses) }, [repo])\n  return courses\n}\n\n// The component only renders already-mapped, validated domain\nfunction CourseList({ repo }: { repo: CourseRepository }) {\n  const courses = useCourses(repo)\n  return courses.map((c) => <CourseCard key={c.id} course={c} />)\n}' },
        { type: 'p', text: 'Notice the symmetry: front and back declare the same CourseRepository port. In a monorepo you can even share the domain entities between both, and the contract stops drifting.' },
        { type: 'h2', text: 'Where these concepts apply' },
        { type: 'p', text: "I won't create domain/application/infrastructure folders in a weekend CRUD. But the moment there are rules that outlive a UI redesign or a database swap, those rules deserve to live isolated. The test is simple: could you swap React for something else, or Prisma for something else, without rewriting your logic? If not, it's too coupled." },
        { type: 'p', text: 'In Tech Academy this meant separating the course domain from its persistence and its UI. When we migrated from fetch to a typed client on the front, and adjusted the ORM on the back, not a single use case changed.' },
      ],
    },
  },
  {
    slug: 'shipping-ai-features',
    date: '2026-04-15',
    readingTime: 9,
    featured: true,
    tags: ['AI', 'Agents', 'Productivity', 'Engineering'],
    cover: 'linear-gradient(135deg, #22d3ee 0%, #6366f1 55%, #a855f7 100%)',
    coverImage: '/images/blog/ia.jpg',
    es: {
      title: 'Programar con IA: del modelo al agente, y cómo sacarle provecho',
      excerpt:
        'Programar con IA cambió cómo trabajo, no solo qué tan rápido escribo. Qué es un modelo, qué es un agente, sus ventajas y desventajas, y cómo orquestar agentes para multiplicar tu impacto como software engineer.',
      body: [
        { type: 'p', text: 'Antes de empezar, sería ilógico pensar en un mundo actual donde la IA no nos atraviese; hoy por hoy estamos ligados a preguntarnos si esto lo escribió un humano o fue algún LLM. El boom que produjo esta revolución tecnológica nos obligó a apalancarnos para mejorar nuestra productividad y así poder crecer a una mayor velocidad, tanto a nivel técnico como humano al momento de tomar decisiones...' },
        { type: 'p', text: 'Programar con IA no es escribir el mismo código más rápido. Es un cambio en cómo trabajo: paso menos tiempo tecleando y más decidiendo qué construir, revisando y dirigiendo. Lo interesante no es la autocompletación, sino que parte del trabajo que antes era mío ahora lo puedo delegar y supervisar.' },
        { type: 'h2', text: 'El impacto real en el día a día' },
        { type: 'p', text: 'El mayor cambio es de rol: de autor a director. La IA aplana la curva de lo tedioso (boilerplate, tests repetitivos, explorar una base de código nueva, traducir entre lenguajes) y te deja energía para lo que de verdad importa: el diseño, las decisiones difíciles y entender el problema. Programas más arriba, no solo más rápido.' },
        { type: 'p', text: 'Pero hay un costo: revisar pasa a ser una habilidad central. Cuando produces más código del que escribiste a mano, el cuello de botella se mueve a leer, entender y validar. El ingeniero que prospera no es el que más teclea, sino el que mejor revisa y dirige.' },
        { type: 'h2', text: 'Qué es un modelo' },
        { type: 'p', text: 'Un modelo es el cerebro: recibe un texto (tu instrucción y el contexto) y devuelve una respuesta. Es una función de una sola pasada (entra texto, sale texto) sin memoria entre llamadas ni capacidad de actuar por su cuenta. Le pides que explique, reescriba, resuma o genere código, y responde con lo que cree que sigue.' },
        { type: 'p', text: 'Sus ventajas: es rápido, predecible y económico. Lo controlas por completo y es fácil de razonar: una pregunta, una respuesta. Sus desventajas: no recuerda, no investiga y no ejecuta nada. Si la tarea necesita varios pasos o información que no le diste, el modelo solo no alcanza (tú tienes que orquestar cada paso a mano).' },
        { type: 'h2', text: 'Qué es un agente' },
        { type: 'p', text: 'Un agente es ese mismo modelo, pero con herramientas y un bucle. En lugar de una sola respuesta, el agente puede leer archivos, buscar, ejecutar comandos o llamar APIs, observar el resultado y decidir el siguiente paso por sí mismo. Repite ese ciclo (pensar, actuar, observar) hasta resolver la tarea o alcanzar un límite.' },
        { type: 'p', text: 'Sus ventajas: resuelve tareas abiertas y de varios pasos sin que lo guíes en cada movimiento. Puede investigar una base de código, hacer un cambio, correr los tests y corregir según lo que falle. Sus desventajas: es más caro (cada paso consume contexto), más lento y más difícil de depurar (puede desviarse, repetir trabajo o tomar un camino equivocado con total confianza). Más autonomía significa menos control directo.' },
        { type: 'quote', text: 'Un modelo responde una pregunta. Un agente persigue un objetivo. La diferencia es cuánta autonomía le das, y cuánto control cedes a cambio.' },
        { type: 'p', text: 'La regla que sigo: empieza con lo más simple que funcione. Si la tarea es acotada y de un paso, un modelo basta. Solo subes a un agente cuando el problema de verdad pide varios pasos, herramientas y decisiones encadenadas. La autonomía es una herramienta, no un objetivo.' },
        { type: 'h2', text: 'Orquestar agentes: de uno a un equipo' },
        { type: 'p', text: 'El salto grande llega cuando dejas de pensar en un agente y empiezas a pensar en varios coordinados. En lugar de un agente gigante que lo intenta todo, divides el trabajo: uno investiga, otro implementa, otro revisa. Cada uno con un rol acotado, igual que un equipo humano. Esto reduce los errores y hace el resultado mucho más fácil de seguir.' },
        { type: 'p', text: 'La orquestación puede ser secuencial (el resultado de uno alimenta al siguiente), en paralelo (varios atacan partes distintas a la vez) o jerárquica (un agente coordinador reparte el trabajo y junta los resultados de los demás). El patrón correcto depende de si los subproblemas son independientes o dependen unos de otros.' },
        { type: 'h2', text: 'Configurar bien a un agente' },
        { type: 'p', text: 'Un agente rinde según cómo lo configuras. Cuatro cosas marcan la diferencia: un rol y objetivo claros (qué hace y qué no), las herramientas justas (darle de más lo distrae y lo vuelve peligroso), el contexto adecuado (qué parte de tu proyecto necesita ver) y límites firmes (máximo de pasos, presupuesto y permisos) para que un error no se convierta en una bola de nieve.' },
        { type: 'p', text: 'En la práctica se parece a hacer onboarding a alguien nuevo: defines su alcance, le das acceso a lo que necesita y no a más, le explicas las convenciones del proyecto y revisas su trabajo antes de fusionarlo. Un agente bien configurado es como un junior incansable; uno mal configurado es ruido caro.' },
        { type: 'h2', text: 'Usa tus conceptos para potenciarte como desarrollador' },
        { type: 'p', text: 'El mayor retorno no está en pedirle a la IA que escriba una función, sino en delegarle trabajo completo y supervisarlo bien. Deja que un agente haga la primera versión de lo rutinario (migraciones, tests, refactors mecánicos, investigar un bug) mientras tú te concentras en la arquitectura y las decisiones que requieren criterio.' },
        { type: 'p', text: 'Tres hábitos que cambian el juego: dar contexto preciso en vez de instrucciones vagas, dividir lo grande en tareas verificables, y tratar cada salida como un PR que hay que revisar, no como verdad. Tu valor ya no es solo escribir código: es definir bien el problema, dirigir el trabajo y garantizar la calidad de lo que entra al repositorio.' },
        { type: 'h2', text: 'Lo que no cambia' },
        { type: 'p', text: 'Trata cada respuesta del modelo como una sugerencia, no como una verdad. Valida entradas y salidas, observa costos, diseña el fallback para cuando se equivoque (porque se va a equivocar) y mantén siempre a un humano en el lazo para lo que importa. La IA bien usada no reemplaza el criterio: lo escala.' },
      ],
    },
    en: {
      title: 'Programming with AI: from model to agent, and how to leverage it',
      excerpt:
        'Programming with AI changed how I work, not just how fast I type. What a model is, what an agent is, their pros and cons, and how to orchestrate agents to multiply your impact as a software engineer.',
      body: [
        { type: 'p', text: 'Before we start, it would be illogical to imagine a present-day world where AI does not run through everything we do; these days we are bound to wonder whether something was written by a human or by some LLM. The boom this technological revolution produced forced us to leverage it to improve our productivity and grow faster, both technically and as people when it comes to making decisions...' },
        { type: 'p', text: 'Programming with AI is not writing the same code faster. It is a shift in how I work: less time typing, more time deciding what to build, reviewing and steering. The interesting part is not autocomplete, but that work that used to be mine can now be delegated and supervised.' },
        { type: 'h2', text: 'The real day-to-day impact' },
        { type: 'p', text: 'The biggest change is in role: from author to director. AI flattens the tedious curve (boilerplate, repetitive tests, exploring a new codebase, translating between languages) and frees energy for what truly matters: design, hard decisions and understanding the problem. You program at a higher level, not just faster.' },
        { type: 'p', text: 'But there is a cost: reviewing becomes a core skill. When you produce more code than you hand-wrote, the bottleneck shifts to reading, understanding and validating. The engineer who thrives is not the one who types the most, but the one who reviews and steers the best.' },
        { type: 'h2', text: 'What a model is' },
        { type: 'p', text: 'A model is the brain: it takes text (your instruction and context) and returns a response. It is a single-pass function (text in, text out) with no memory between calls and no ability to act on its own. You ask it to explain, rewrite, summarize or generate code, and it replies with what it thinks comes next.' },
        { type: 'p', text: 'Its strengths: fast, predictable and cheap. You control it fully and it is easy to reason about: one question, one answer. Its weaknesses: it does not remember, research or run anything. If the task needs several steps or information you did not give it, the model alone falls short (you have to orchestrate every step by hand).' },
        { type: 'h2', text: 'What an agent is' },
        { type: 'p', text: 'An agent is that same model, but with tools and a loop. Instead of a single answer, the agent can read files, search, run commands or call APIs, observe the result and decide the next step on its own. It repeats that cycle (think, act, observe) until it solves the task or hits a limit.' },
        { type: 'p', text: 'Its strengths: it solves open-ended, multi-step tasks without you guiding every move. It can investigate a codebase, make a change, run the tests and fix based on what fails. Its weaknesses: it is more expensive (each step consumes context), slower and harder to debug (it can drift, repeat work or confidently take a wrong path). More autonomy means less direct control.' },
        { type: 'quote', text: 'A model answers a question. An agent pursues a goal. The difference is how much autonomy you grant, and how much control you give up in return.' },
        { type: 'p', text: 'The rule I follow: start with the simplest thing that works. If the task is bounded and single-step, a model is enough. You only move up to an agent when the problem genuinely needs multiple steps, tools and chained decisions. Autonomy is a tool, not a goal.' },
        { type: 'h2', text: 'Orchestrating agents: from one to a team' },
        { type: 'p', text: 'The big leap comes when you stop thinking about one agent and start thinking about several coordinated ones. Instead of one giant agent attempting everything, you split the work: one researches, another implements, another reviews. Each with a narrow role, just like a human team. This reduces errors and makes the outcome far easier to follow.' },
        { type: 'p', text: 'Orchestration can be sequential (one output feeds the next), parallel (several attack different parts at once) or hierarchical (a coordinator agent delegates the work and merges the others results). The right pattern depends on whether the subproblems are independent or depend on each other.' },
        { type: 'h2', text: 'Configuring an agent well' },
        { type: 'p', text: 'An agent performs as well as you configure it. Four things make the difference: a clear role and goal (what it does and does not), the right tools (giving it too many distracts it and makes it dangerous), the right context (what part of your project it needs to see) and firm limits (max steps, budget and permissions) so one mistake does not snowball.' },
        { type: 'p', text: 'In practice this looks like onboarding someone new: you define their scope, give access to what they need and no more, explain the project conventions and review their work before merging. A well-configured agent is like a tireless junior; a badly configured one is expensive noise.' },
        { type: 'h2', text: 'Use your concepts to empower yourself as a developer' },
        { type: 'p', text: 'The biggest return is not asking AI to write a function, but delegating whole pieces of work and supervising them well. Let an agent draft the first version of the routine stuff (migrations, tests, mechanical refactors, investigating a bug) while you focus on architecture and the decisions that require judgment.' },
        { type: 'p', text: 'Three habits that change the game: give precise context instead of vague instructions, split big work into verifiable tasks, and treat every output as a PR to review, not as truth. Your value is no longer just writing code: it is defining the problem well, steering the work and guaranteeing the quality of what lands in the repo.' },
        { type: 'h2', text: 'What does not change' },
        { type: 'p', text: 'Treat every model response as a suggestion, not a truth. Validate inputs and outputs, watch costs, design the fallback for when it gets it wrong (because it will) and always keep a human in the loop for what matters. Used well, AI does not replace judgment: it scales it.' },
      ],
    },
  },
]

export const getPost = (slug) => posts.find((p) => p.slug === slug) || null
